import 'package:flutter/material.dart';

/// Bambare brand accent (replaces former green #3FB885 across admin UI).
abstract final class BrandColors {
  static const accent = Color(0xFFFFD40D);
  static const onAccent = Color(0xFF1B1B2F);
}
